import React from "react";

const Card = (propos) => {
  console.log(propos);
  return (
    <li className="card">
      <div className="infos">
        <h2>nom du pays</h2>
      </div>
    </li>
  );
};

export default Card;
